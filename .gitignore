# Bytecode / caches
__pycache__/
*.py[cod]
*.pyo
.Python
*.so

# Virtual environments
.venv/
venv/
env/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Tooling / coverage caches
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/

# Jupyter
.ipynb_checkpoints/

# Local experiments / scratch notebooks (not part of the engine)
brainstorm.ipynb

# Training artifacts — too large for git (498MB .npz exceeds GitHub's 100MB
# limit) and re-derivable from the extract/train scripts.
data/
checkpoints/
*.npz
*.pt

# Rust build output (Cargo.lock IS committed — this is a binary crate)
engine-rs/target/

# Match/analysis outputs (transient experiment artifacts, not source)
*.pgn
*.bin

# API keys / secrets
apis.txt
